import React from 'react'
import { EuiButton } from '@elastic/eui'

type CreatePartyProps = {
  //
}
const CreateParty = ({}: CreatePartyProps) => {
  return <EuiButton>Create party</EuiButton>
}

export default CreateParty
